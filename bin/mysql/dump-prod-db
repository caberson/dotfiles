# Update the variables.
host=""
db=""
user=""
pwd=""

dumpfile="`eval date +%Y%m%d`.prod.sql"
dumpfilepath="${PWD}/var/dumps/$dumpfile"
echo $dumpfilepath

~/bin/dump-db -h$host -d$db -u$user -p$pwd > $dumpfilepath